# dynamic.yml
http:
  routers:
    user-service:
      rule: "Host(`localhost`)"
      service: user-service
      entryPoints:
        - web
  services:
    user-service:
      loadBalancer:
        servers:
          url: "http://user-service:8081"
          #- url: "http://host.docker.internal:8081"
